[project]
name = "douyin-computer-use-poc"
version = "0.1.0"
description = "“全网找‘屎’专家” - safe-by-default Douyin computer-use proof of concept"
readme = "README.md"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-q"
